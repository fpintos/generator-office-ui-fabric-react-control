import * as React from 'react';
import { <%= name %> } from '../../<%= name %>';

export class <%= name %>BasicExample extends React.Component {
  public render(): JSX.Element {
    return (
      <<%= name %> />
    );
  }
}
